Optional files:

piano-relax.mp3  — Background piano during practice
pingshuai.gif    — Optional real demo GIF (not required; app uses built-in figure)

Run once for Expo icons:
  node scripts\create-placeholder-assets.js

Creates: icon.png, splash.png, adaptive-icon.png
