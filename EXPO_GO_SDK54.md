# Expo Go SDK 54 — quick steps

```cmd
cd /d D:\LiMVPapp\MVP1PingShui
npm install
npx expo install --fix
node scripts\create-placeholder-assets.js
npm run start
```

Phone: Expo Go **SDK 54** → Scan QR.

**Spinner won't stop?** Laptop can't reach phone → use tunnel:

```cmd
npm run start:tunnel
```

Allow **Node.js** in Windows Firewall (Private network).
